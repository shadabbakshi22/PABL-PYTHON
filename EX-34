class Solution:
    def findMedian(self, arr):
        arr.sort()
        n = len(arr)

        # odd length
        if n % 2 == 1:
            return arr[n // 2]

        # even length
        return (arr[n//2 - 1] + arr[n//2]) / 2
